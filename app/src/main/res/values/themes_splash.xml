<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 시스템 스플래시 (즉시 종료) -->
    <style name="Theme.Bus.Splash" parent="Theme.SplashScreen">
        <!--  화면 배경색 (activity_splash.xml과 동일하게 설정할 것) -->
        <item name="windowSplashScreenBackground">#3F51B5</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_empty</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="windowSplashScreenAnimationDuration">1</item>
        <item name="postSplashScreenTheme">@style/Theme.Bus</item>
    </style>
</resources>

